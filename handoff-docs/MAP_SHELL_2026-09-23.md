# Map shell — resort bottom sheet + mobile header (2026-09-23)

Package: `sheet` (audit themes "Resort bottom sheet redesign" C3 and
"Mobile map header & bottom-pill cleanup" C2). Branch `wf/sheet-r3`,
from `feat/season-1-round2-clean` @ 3633b1f. No schema, no new npm deps.

## What shipped

| Area | Change |
|---|---|
| `components/Map/ResortSheet.tsx` (new) | Phone sheet shell: three snap points, 36 x 5 px handle on a 44 px hit area, drag on handle and hero, flick-to-close, Google's nested-scroll rule, live height feed, history entry for the back gesture, reduced-motion aware. Hand-rolled (see "Why not vaul"). |
| `components/Map/ResortSheetMath.ts` (new) | Pure snap geometry, gesture resolution (incl. the mid-gesture scroll to drag handover), directions URL, chrome budget constants, phone bottom-row layout. Tested in `tests/resortSheetMath.test.ts`. |
| `components/Map/sheetHistory.ts` (new) | Pure history-entry rules for the sheet and for `MapPage.writeQuery` (what a UI close does, which `history.state` keys a URL rewrite keeps). Tested in `tests/sheetHistory.test.ts`. |
| `lib/glanceTiles.ts` (new) | `buildGlanceTiles`: the four at-a-glance tiles, shared by the sheet and `/resort/[slug]` so labels and source words are identical. Tested in `tests/glanceTiles.test.ts`. |
| `lib/hitArea.ts` (new) | `HIT_AREA_44*` class strings: invisible `::before` hit extensions so 32-36 px chips keep a 44 px tap target. |
| `components/Map/ResortSheetContent.tsx` (new) | Shared content: hero backdrop (photo or pass gradient + grain), 4-tile stat row, pass chips with per-product rules, docked action bar (Plan trip gold / Directions / Save / Compare on phones / Share), status pills, "Top picks nearby". Used by both the phone sheet and the desktop rail. |
| `components/Map/ResortPanel.tsx` | Now the orchestrator: resolves drive time (Matrix upgrade), status (`lib/seasonDates`), tiles; renders `ResortSheet` on phones and the 380 px rail on desktop from the same pieces. |
| `components/Map/MapPage.tsx` | Header: ONE 44 px row (brand + Search / Plan / Saturday / Filters / Account as 44 px buttons with 11 px labels) + ONE 40 px pass-chip strip + ONE 44 px secondary row (Today / active trip / recents, only when one of them rendered) + a 32 px founder strip. Owns sheet snap + settled height, publishes `--wn-bottom-stack` and `data-sheet-snap`, the phone bottom pill row (List, Compare, Location), bare-map tap collapses the sheet to peek, reopens the sheet when history restores it; `writeQuery` keeps the sheet's history keys. |
| `components/Map/MapView.tsx` (minimal, additive) | New optional prop `resortSheetPadding`: the open-pin camera and fly-to use the live sheet height instead of a fixed 50 vh, and a settled snap change eases the padding (0 ms under reduced motion). |
| `components/Map/MobileQuickFilters.tsx` | 40 px row, 36 px chips inset from the edge, no visible scrollbar, order Ikon / Epic / Indy / Mtn Collective / No pass, counts in tabular numerals. |
| `components/RecentlyViewedStrip.tsx` | Exports `RecentChips` (bare 36 px chips for the secondary row); the desktop inline strip is unchanged. |
| `components/Map/TodayChip.tsx`, `ActiveTripChip.tsx` | Pill only (no wrapper row); 36 px visual, 44 px hit area. |
| `components/OffSeasonBanner.tsx` | 32 px full-width strip; the dismiss button is 44 x 44 and overhangs the strip. |
| `components/CompareFloatingButton.tsx`, `components/Map/LocationButton.tsx` | Phones: `ComparePill` and `LocationButton placement="row"` render inside MapPage's bottom pill row. Desktop: the floating pills keep `md:bottom-10` / `md:bottom-28` (no `--wn-bottom-stack` offset there). |
| `components/CompareToggle.tsx` | New `variant="action"` (icon over label, 44 px) for the phone sheet's action bar. |
| `app/resort/[slug]/page.tsx` | At-a-glance strip built by `lib/glanceTiles` (same labels as the sheet); 11 px labels at 75 % charcoal. |
| `app/page.tsx` | `weather_cache` select adds `fetched_at` so the tiles can say "Forecast · 3h ago"; the resort select adds `hero_image_attribution` (column from `data/stage-25-schema.sql`, already read by the resort page) so the sheet hero can credit CC BY / BY-SA photos. |

## Snap points (`ResortSheetMath.snapHeights`)

| Snap | Height | Shows |
|---|---|---|
| peek | 140 px | Hero strip: name, state · region, status pill, drive from origin, one key stat (new snow > % open > vertical > trails). Map fully pannable. |
| half | 52 % of `innerHeight` (min peek + 120, max full − 80) | Hero + 4-tile stat row + drive source line + pass chips; action bar docked at the bottom of the visible sheet. |
| full | 92 % of `innerHeight` | Hero collapses to a 56 px title bar (photo fades, name shrinks, × stays), body scrolls (pass rules, countdown, nearby picks), map sliver stays visible at the top. |

Gestures: drag on the handle or the hero anywhere; flick (≥ 0.45 px/ms)
moves one snap in the flick direction, a downward flick from peek closes;
slow release snaps to the nearest; releasing under 70 px closes. Body:
below full any vertical move drags the sheet (drag up expands before
content scrolls); at full an upward move scrolls the content and a
downward move drags the sheet only when the content is at the top.
Sideways swipes go to the horizontal strips inside. Keyboard: the handle
is a button, arrow up / down resize, Escape closes (MapPage). Fresh open
lands on half; pin → pin taps keep the current snap; a tap on bare map
collapses to peek (MapView reports bare-map taps only: pins, clusters,
DOM markers and controls never count); the phone back gesture closes.
At full, a downward scroll that reaches the top of the content hands
over to a sheet drag inside the same gesture (no second pull needed).

History: opening pushes ONE entry with the same URL and
`{ wnSheet: id, wnSheetHref: href }`; switching resorts rewrites the id
in place. A UI close (×, Escape, flick, ArrowDown, unmount) calls
`history.back()` only while that entry is on top AND the URL still equals
`wnSheetHref`. After a filter or origin tap has rewritten the URL, going
back would restore the pre-filter URL, so the close strips our keys with
`replaceState` instead and the filter stays (`sheetHistory.sheetCloseAction`).
`MapPage.writeQuery` passes the non-Next part of `history.state` through,
so the back gesture still closes the sheet after filter taps. Back from
`/resort/[slug]` restores the sheet from `history.state.wnSheet`.

Reduced motion: no slide-in animation, no height / hero transition, map
padding jumps instead of easing.

## Map padding

`ResortPanel` forwards only settled heights; `MapPage` converts them with
`sheetMapPadding` (peek → 140, half → 52 %, full → holds the half value
because the sheet covers the map anyway) and passes `resortSheetPadding`
to `MapView`, which eases the camera padding so the selected pin stays in
the visible sliver. Desktop keeps the 380 px right padding.

## Bottom stack (`--wn-bottom-stack`)

Defined on the map root (`[data-route="map"]`), in px from the container's
bottom edge (the container already excludes the phone tab bar via
`--wn-tab-bar-h` in `app/globals.css`):

- 40 px: the Mapbox attribution + wordmark band (terms requirement)
- 148 px: while the install nudge (`[role=region][aria-label="Install Wynla"]`) is in the DOM
- peek sheet height + 12 px: while a resort sheet is at peek

`data-sheet-snap="peek|half|full"` is set on the same root while a phone
sheet is open. Consumers: `bottom: var(--wn-bottom-stack, 40px)` and hide
under `[data-sheet-snap=half]` / `[data-sheet-snap=full]` (Tailwind:
`[[data-sheet-snap=half]_&]:hidden [[data-sheet-snap=full]_&]:hidden`).
On phones List, Compare and Location are ONE flex row
(`justify-end gap-1.5`, `pl-[132px]` reserving the Feedback pill's slot)
anchored at the variable, so they cannot overlap. Location is a 44 px icon
whenever List or Compare shows; List is a 44 px icon while Compare shows
(`phoneBottomRow`, width asserted for 360 and 375 px phones).
**FeedbackButton (a11y package) should switch from `bottom-10` to the
variable and the two hidden variants; until then it stays at 40 px and is
covered by a peek sheet.**

## Chrome budget (375 x 812, `ResortSheetMath.MOBILE_CHROME`)

| Row | Height |
|---|---|
| Gap under the status bar | 8 |
| Primary row | 44 |
| Pass chips | 40 |
| Secondary row (only when Today / trip / recents rendered; absent on a first visit) | 44 |
| Founder banner (May–Oct, until dismissed) | 32 |
| **Total** | **168 px** (92 px with only the two mandatory rows) |

Measured from the row heights, which are fixed with `h-11` / `h-10` /
`h-8` rather than padded content; `tests/resortSheetMath.test.ts`
asserts the sum stays under 170. At runtime the same number is published
as `--wn-header-h` on the map root (ResizeObserver), read it in DevTools
with `getComputedStyle(document.querySelector('[data-route=map]')).getPropertyValue('--wn-header-h')`
(add `env(safe-area-inset-top)` on a notched phone: the budget excludes
the status bar, which the map shows through).

## Why not vaul

Evaluated against audit benchmark #1. It does not expose the in-flight
drag height (the map padding must follow the settled snap and the pill
stack the peek height), its non-modal mode still installs body scroll
locks that fight Mapbox, and it adds `@radix-ui/react-dialog` to the map
bundle. The gesture model here is ~150 lines with every branch in pure,
tested helpers.

## How to test on iPhone

1. Open https://wynla.app (or the preview) in Safari and in the installed app.
2. Tap a pin: the sheet opens at half with the photo hero, 4 tiles (each with Measured / Reported / Forecast and an age), and the gold Plan trip bar at the bottom. The map eases so the pin sits above the sheet.
3. Drag the hero up: it reaches full, the photo fades to a white 56 px title bar, a map sliver stays visible, and only now does the body scroll. Scroll down, then scroll back to the top and keep pulling: the sheet collapses.
4. Flick down from half: peek (name, status, drive, one stat). Flick down again: closes. Tap a pin while at peek: the sheet stays at peek with the new resort.
5. Tap bare map with the sheet at half: it collapses to peek. Swipe back from the screen edge with the sheet open: it closes and you are still on the map.
6. Tap "Full resort page →", then swipe back: the same sheet is open again.
7. Header: one button row (Search / Plan / Saturday / Filters / Account, 44 px buttons with 11 px labels), one chip row, the shortcut row only when it has pills, the founder strip; nothing wraps or clips at 375 px (check the account button is fully visible when signed out). Chips scroll with no scrollbar.
8. Bottom: List (with count) and a round Location button share one row right of Feedback. Add two resorts from the sheet's Compare button and the row becomes List icon, Compare 2, Location icon with no overlap. With the install nudge showing the row moves above it; with a sheet at peek it sits on its shoulder; at half / full it hides.
9. History: tap a pin, tap the Ikon chip, tap ×: the Ikon filter stays. Tap a pin, tap Epic, swipe back: the sheet closes and you are still on the map.
10. Settings → Accessibility → Motion → Reduce Motion: the sheet appears and resizes without animation.
11. VoiceOver: the handle reads "Resize resort sheet (half). Arrow keys resize, Escape closes."; every button in the header and action bar has a name.

## Known limits / follow-ups

- The back gesture after a filter tap pops the sheet's entry, and Next restores the entry underneath, whose URL predates the filter tap. Only UI closes are protected; a back press reverting the last filter is ordinary browser behaviour on this page.
- Map padding follows the SETTLED snap, not the live drag height: re-padding Mapbox on every drag frame moves the camera under the finger and costs a frame each time. Deliberate; revisit only if a device test shows the pin hidden after a drag.
- Not exercised in a browser or on a device: the touch model (the 6 px slop vs the browser's own pan decision, the mid-gesture handover with `overscroll-none`), the `:has()` secondary row, the header row width at 360 / 375 px, the 168 px chrome total (computed from fixed row heights, not measured).
- `snow_new_24h_in` written by the daily NOHRSC / SNOTEL analysis has no stamp of its own; the tile dates it by the weather sync (`weather_cache.fetched_at`, "Measured · 3h ago") or says "Measured · daily" when the payload lacks it.
- Reopen-from-history also fires after a hard reload of the map with a sheet entry on top (state survives reloads), which matches Google Maps.
- FeedbackButton still uses `bottom-10` (a11y package owns it): see the bottom-stack note above.
