# Install Wynla — one-tap install + installed-app polish (2026-09-23)

Package: `install` (audit themes "One-tap Install button" and "Installed app polish").
Branch: `feat/install-pwa`, branched from `d19e517` on `feat/season-1-core`.

## What shipped

| Area | Change |
|---|---|
| `public/manifest.json` | `id: "/"`, `scope: "/"`, `start_url: "/?source=pwa"`, `display_override`, `lang`, `launch_handler`, `related_applications` self-reference, a real maskable icon (`/icon-maskable-512.png`, mark inside the 80 % safe zone), 3 narrow + 1 wide screenshots (`/screenshots/*.png`) so Chrome shows the richer install sheet, 3 shortcuts with 96 px icons. |
| `public/sw.js` | Versioned caches (`VERSION`), network-first navigations with `/offline` fallback, cache-first for `/_next/static/*`, icons, manifest, splash and screenshots. HTML is never cached (most routes are personalised). Old caches deleted on activate. `pushsubscriptionchange` re-subscribes (key from the old subscription, else the VAPID key the page hands over via `postMessage`) and POSTs to `/api/push/subscribe`. |
| `components/PwaRegistrar.tsx` | Registers the worker, hands it the VAPID public key, re-POSTs the current push subscription once per browser session (keeps `last_seen_at` fresh and binds anonymous subscriptions to the signed-in user), reloads exactly once when a new worker takes control. |
| `components/InstallPrompt.tsx` | New. Four modes — A Chromium native prompt, B iOS guided sheet (per browser, iOS 26 aware), C in-app browser escape (`x-safari-https://`, `intent://`, copy link), D installed → nothing. Exports `InstallPrompt` (global nudge + installed toast), `InstallCard` (/get), `InstallRow` (/account). Also strips `?source=pwa|shortcut` from the address bar after launch. |
| `app/layout.tsx` | Inline `<head>` script stashes `beforeinstallprompt` before hydration; `appleWebApp.startupImage` now lists 10 iPhone launch images with media queries (replaces the single unmatched `<link>`); `InstallPrompt` mounted; `IosInstallBanner` removed. |
| `app/get/page.tsx` | New landing page for bio links and escape buttons. `?from=<app>` auto-opens the guide after an escape. |
| `app/offline/page.tsx` | New branded offline page (retry, auto-retry on `online`, links for when the connection is back). Precached by the worker. |
| `app/globals.css` | One rule pads `#main-content` by `env(safe-area-inset-top)` on every non-map route; the map opts out via `.route-map` and `:has([data-route="map"])`. Per-page hacks on `/compare` and `/resort/[slug]` were removed so nothing is double padded. |
| `scripts/gen-brand-rasters.mjs` | Emits the maskable icon, `icon-96.png`, the iOS splash set and the manifest screenshots. Re-run with `node scripts/gen-brand-rasters.mjs` after any brand change. |

## Nudge rules (global `InstallPrompt`)

- Counts one visit per browser session (`sessionStorage`), stores the total in `localStorage` (`wynla_install_visits`).
- Shows the top toast on the 2nd visit or later, 8 s after load, at most once per 7 days (`wynla_install_nudge_at`).
- Never on `/login`, `/auth/*`, `/trip/*` (shared trip links), `/lists/*`, `/get`, `/offline`, nor inside the installed app.
- On the map it sits 68 px below the safe area so it clears the search/filter row; elsewhere 12 px.
- Dismiss hides it for the current load; the 7-day timer is what stops it coming back.

## How to test

### Android Chrome (mode A)
1. Open https://wynla.app in Chrome, visit twice (close the tab in between) or go to `/get`.
2. Tap **Install Wynla**. Chrome shows the richer install sheet with the three screenshots and the description. Tap **Install**.
3. Expect the "Installed. Open Wynla from your home screen." toast, a WebAPK icon in the app drawer, and the button gone from `/get` and `/account`.
4. `chrome://inspect` → Application → Manifest shows no installability warnings; Service Workers shows `sw.js` with the current `VERSION` caches.
5. Airplane mode → open the app → the branded `/offline` page shows; turning the network back on reloads automatically.
6. Samsung Internet 27+ never fires `beforeinstallprompt`: the same button opens the generic menu guide, which is correct.

### iPhone Safari (mode B)
1. Open https://wynla.app in Safari; go to `/get` or wait for the nudge.
2. Tap **Add to home screen** → sheet says: Share (bottom bar; on iOS 26 behind the three dots) → Add to Home Screen → Add, and warns that the installed app needs its own sign-in.
3. Follow the steps; the launch image is navy with the mark; the status bar overlays the navy header on the map, and every other page starts below the clock (check `/trips`, `/favorites`, `/account`, `/login`, `/compare`, `/resort/vail`).
4. Chrome / Firefox / Edge on iOS get their own share-button location in step 1.
5. Sign in inside the installed app once. Magic links from Mail still open in Safari, not the app — that fix (email code login) belongs to the auth package.

### TikTok / Instagram in-app browser (mode C)
1. Put `https://wynla.app/get` in the TikTok bio, open it from the TikTok app.
2. The card reads "You're inside TikTok"; tap **Open outside TikTok** → **Open in Safari** (iOS, `x-safari-https://`) or **Open in Chrome** (Android, `intent://`).
3. The page reopens in the real browser at `/get?from=tiktok` and, on iOS, the guided sheet opens by itself.
4. If the deep link is blocked (Instagram on iOS is the usual offender), **Copy link** and the "menu → Open in browser" hint are the fallbacks.

### Desktop Chrome / Edge
`/get` → **Install Wynla** → native dialog → app window. Firefox gets the generic guide (Firefox desktop has no PWA install).

### Service-worker update
Deploy a new build with a bumped `VERSION`: an open tab reloads once when the new worker activates; no reload loop (the reload is gated on a controller having existed).

## Play Store (TWA) path — later

Prerequisites are now met: manifest with `id`/`scope`/screenshots, maskable icon, and a real fetch handler with offline fallback (Play policy 4.3 rejects "thin wrappers" without one).

1. Google Play developer account ($25 once). Personal accounts created after 2023-11-13 must run a closed test with 12 testers for 14 days before production.
2. `npx @bubblewrap/cli init --manifest https://wynla.app/manifest.json`, then `npx @bubblewrap/cli build` (or PWABuilder → Android package). Package name suggestion: `app.wynla.twa`.
3. Publish `/.well-known/assetlinks.json` with the signing certificate's SHA-256 (Bubblewrap prints it). Without it the TWA shows a browser toolbar.
4. Keep the listing screenshots in sync with `/screenshots/*.png` (or capture real UI once the map redesign is final).
5. iOS App Store wrapper stays deferred (Guideline 4.2 risk, needs Mac + Xcode + $99/yr); revisit post-OPT per the mobile strategy.

## Not in this package

- Post-install "you're in the app" tip + notification pre-prompt (pwa-install-27 item 6) — add once the auth package lands email-code login, since the first in-app action is signing in.
- Per-route `theme-color` (pwa-install-18) — needs `viewport` exports on pages outside this package's file scope.
- In-app splash skip when standalone (pwa-install-15) — `components/Map/MapPage.tsx`, map package.
- Keyboard-aware sheets (mobile-ergonomics-13/14) — `ResortPicker` / `FeedbackButton`, out of scope.
- Analytics events for shown / accepted / dismissed / installed — no event pipeline exists yet; `?source=pwa` on `start_url` gives Vercel Analytics the launch count.
